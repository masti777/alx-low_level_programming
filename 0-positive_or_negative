#include <stdlib.h>
#include <time.h>
/* more headers goes there */

/* betty style doc for function main goes there */
int main(void)
{
srand(time(0));
        n = rand() - RAND_MAX / 2;

	int n,c,i;
        c-i-'0';
for (c='0'; c<='9'; c++)
{
   for (i='0';i<='9'; i++)
{
     putchar(c);
     putchar(i);
       if ((c!='9') || (c=='9' && i !='9'))
{
   putchar(',');
   putchar(',');
}

}

}
putchar('/n');
return(0);
}

	
	return (0);
}
